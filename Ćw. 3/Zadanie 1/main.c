#include "zeros.h"

int main(void){
    x = 2.0;
    y = 5.0;
    z = -6.0;
    zeros(x,y,z);

    x = 1;
    y = 2;
    z = 3;
    zeros(x,y,z);

    x = 5;
    y = -6;
    z = 10;
    zeros(x,y,z);


    return 0;
}